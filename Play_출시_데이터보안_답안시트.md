# Play Console 데이터 보안 · 앱 액세스 답안 시트

앱 이름: **부산 땅따먹기 (BusanQuest)**
패키지명: **`kr.co.busanquest`**  ← 2026-08-20 `com.example.busasnquest` 에서 변경 (게시 후 변경 불가)
개발자: **삠삠** / 문의: **sihyunb88@gmail.com**

> Play Console 에서 그대로 보고 옮겨 적을 수 있게 정리했다.
> "🔴 미완료" 표시가 남아 있으면 **제출하면 안 된다.** 실제 앱 동작과 양식이 다르면 반려된다.

---

## 0. Play Console 에 넣을 URL

| 입력 위치 | URL |
|---|---|
| 앱 콘텐츠 → 개인정보처리방침 | `https://parksh23.github.io/2026_Visiting_Data/privacy.html` |
| 앱 콘텐츠 → 데이터 보안 → 데이터 삭제 | `https://parksh23.github.io/2026_Visiting_Data/delete-account.html` |

🔴 **GitHub Pages 를 먼저 켜야 한다.** 저장소 Settings → Pages → Source: `main` 브랜치 `/docs` 폴더.
켠 뒤 두 URL 이 실제로 열리는지 반드시 확인할 것 (Google 이 "제대로 작동" 을 조건으로 명시).

---

## 1. 데이터 보안 — 전체 질문

| 질문 | 답 |
|---|---|
| 이 앱이 사용자 데이터를 수집하거나 공유하나요? | **예** |
| 수집하는 모든 사용자 데이터가 전송 중에 암호화되나요? | **예** |
| 사용자가 데이터 삭제를 요청할 수 있는 방법을 제공하나요? | **예** (위 URL) |
| 앱에 광고 ID 를 사용하나요? | **아니요** |

**"전송 중 암호화 = 예" 의 근거**
- API 통신은 전부 HTTPS (`https://visiting-data.onrender.com`)
- 매니페스트의 `usesCleartextTraffic="true"` 는 제거했고, 대신 `network_security_config.xml` 로
  평문을 **기본 차단**했다. 예외는 관광공사 이미지 CDN(`tong.visitkorea.or.kr`) 한 곳뿐이고
  **이 도메인으로는 사용자 데이터를 보내지 않는다**(이미지를 받기만 함)

---

## 2. 데이터 유형별 답안

**공유(shared)는 전부 "아니요"** — 판단 근거는 §4 참고.
**목적은 전부 "앱 기능" 또는 "계정 관리"** — 분석·광고·개인 맞춤설정 없음.

| Play 데이터 유형 | 수집 | 공유 | 필수 여부 | 목적 | 실제 값 |
|---|:-:|:-:|---|---|---|
| 개인 정보 → **이메일 주소** | 예 | 아니요 | 필수 | 계정 관리 | 회원가입 이메일 |
| 개인 정보 → **이름** | 예 | 아니요 | 필수 | 계정 관리, 앱 기능 | 닉네임 (랭킹에 표시) |
| 개인 정보 → **사용자 ID** | 예 | 아니요 | 필수 | 계정 관리 | `USER_CODE`, 카카오 회원번호 |
| 개인 정보 → **기타 정보** | 예 | 아니요 | 필수 | 계정 관리 | 비밀번호 (해시 저장) ※§4 참고 |
| 위치 → **정확한 위치** | 예 | 아니요 | **선택** | 앱 기능 | 미션 인증 시점의 좌표, 사진 EXIF 좌표 |
| 사진 및 동영상 → **사진** | 예 | 아니요 | **선택** | 앱 기능 | 미션 인증 사진, 영수증 이미지 |
| 앱 활동 → **앱 상호작용** | 예 | 아니요 | 필수 | 앱 기능 | 완료 미션, 포인트, 구·군 점령 현황, 찜 목록 |

**"선택" 으로 답하는 이유** — 위치와 사진은 미션 인증을 시도할 때만 전송된다.
로그인 후 미션 목록·지도·랭킹만 보는 것도 가능하므로 앱 사용의 필수 조건이 아니다.

### 수집으로 신고하지 **않는** 것

Google 정의상 수집은 `"transmitting data from your app off a user's device"` 다.
기기 밖으로 안 나가면 신고 대상이 아니다.

| 항목 | 이유 |
|---|---|
| 알림 설정 스위치 5종 | DataStore 로컬 저장. 서버로 안 보냄 |
| 앱 버전·기기 모델·OS 버전 | 문의 메일 본문에 **사용자가 직접** 넣어 보내는 값. 앱이 서버로 전송하지 않음 |
| 최근 알림 판단용 값 (본 미션 ID, 마지막 순위) | 로컬 저장 |

⚠️ **백엔드 4차 요청서 §5(알림 설정 서버 저장)를 진행하면 이 표가 바뀐다.**
서버에 저장하는 순간 "앱 활동 → 기타 작업" 또는 "앱 정보 → 기타" 로 **수집 체크가 추가**돼야 한다.
양식과 코드가 어긋나는 대표 케이스이니 그때 이 문서도 같이 고칠 것.

---

## 3. 데이터 삭제 질문

| 질문 | 답 |
|---|---|
| 개발자가 계정 삭제 요청을 받을 수 있는 방법을 제공하나요? | **예** |
| 계정 삭제 URL | `.../delete-account.html` |
| 앱 내 계정 삭제 경로가 있나요? | 🔴 **아직 없음 — 반드시 만들고 제출** |

Google 은 **인앱 경로와 웹 링크를 둘 다** 요구한다.

> "앱 계정 및 관련 데이터를 삭제할 수 있는 인앱 경로를 제공합니다.
>  사용자가 앱 계정 삭제 및 관련 데이터 삭제를 요청할 수 있는 웹 링크 리소스를 제공합니다."

- 웹 링크: ✅ `delete-account.html` 작성 완료
- 인앱 경로: 🔴 [내 정보 > 계정 설정 > 회원 탈퇴] 가 아직 "준비 중" 배지 상태.
  백엔드 `DELETE /api/v1/users/me` (4차 요청서 §2) 가 나와야 완성된다
- 소프트 삭제(`ACCOUNT_STATUS='WITHDRAWN'`)만으로는 부족하다. **인증 사진은 실제로 삭제**해야
  `delete-account.html` 에 적어둔 내용과 앞뒤가 맞는다

---

## 4. 판단 근거 메모 (반려 대응용)

**Q. Gemini API 로 인증 사진을 보내는데 "공유" 아닌가?**

아니다. Google 의 공유 예외에 해당한다.

> "'Service provider' means an entity that processes user data on behalf of the developer
>  and based on the developer's instructions."

Gemini(Google), Render(호스팅), Oracle Cloud(DB) 모두 **개발자를 대신해 처리하는 서비스 제공업체**라
데이터 보안 양식에서는 공유로 신고하지 않는다.
**단, 개인정보처리방침에는 처리위탁으로 반드시 기재해야 한다**(한국 개인정보보호법 요구, 이미 기재 완료).

**Q. 비밀번호는 어떤 유형인가?**

Play 데이터 유형 목록에 비밀번호 전용 항목이 없다. 개인 정보 → **"기타 정보"** 로 신고하고
설명에 "계정 인증용 비밀번호(해시 저장)" 라고 적는 것이 무난하다. 확신이 필요하면 제출 전
Play Console 의 각 유형 설명을 한 번 더 확인할 것.

**Q. SDK 가 수집하는 것도 내 책임인가?**

그렇다. 앱에 포함된 라이브러리/SDK 가 전송하는 데이터도 개발자가 신고해야 한다.

🔴 **제출 전 확인 필요:**
- 카카오 로그인 SDK (`v2-user`) 가 수집·전송하는 항목
- 카카오맵 SDK (`com.kakao.maps.open`) 가 수집·전송하는 항목
- FCM 을 붙이면 **기기 ID 류가 추가될 가능성이 높다** → 붙인 뒤 이 표를 다시 볼 것

---

## 5. 앱 액세스 정보 (App access)

| 질문 | 답 |
|---|---|
| 앱의 모든 기능을 제한 없이 사용할 수 있나요? | **아니요** — 로그인 필요 |

**리뷰어에게 제공할 정보**

| 항목 | 값 |
|---|---|
| 안내 이름 | 이메일 로그인 |
| 사용자 이름 | 🔴 (테스트 계정 이메일 — 생성 필요) |
| 비밀번호 | 🔴 (테스트 계정 비밀번호 — 생성 필요) |

**기타 안내에 반드시 적을 내용**

```
· 카카오 로그인 대신 위 이메일 계정으로 로그인해 주세요.
  (카카오 로그인은 카카오 계정이 필요해 리뷰에 적합하지 않습니다)

· 이 앱은 부산 지역 관광지 방문 인증 앱입니다.
  '현재 위치 인증'과 '사진 위치 인증'은 미션 장소 반경 300m 안에서만 통과합니다.
  부산 이외 지역이나 에뮬레이터에서는 위치 인증이 실패하는 것이 정상 동작입니다.

· 위치 인증 없이도 홈 / 미션 목록 / 지도 / 랭킹 / 내 정보 화면은 모두 확인하실 수 있습니다.

· 테스트 계정에는 완료 미션과 포인트를 미리 넣어두었습니다.
```

🔴 **위치 인증이 리뷰에서 실패하는 건 정상이지만, 안내를 안 적으면 "기능이 작동하지 않음" 으로
반려될 수 있다.** 위 문구를 꼭 넣을 것.

---

## 6. 제출 전 체크리스트

**앱 코드 (프론트)**
- [x] 패키지명 `kr.co.busanquest` 로 변경
- [x] `usesCleartextTraffic` 제거 → `network_security_config.xml` 로 대체
- [x] 중복 INTERNET 권한 정리
- [x] 문의 메일 실제 주소로 교체
- [ ] 🔴 [내 정보 > 계정 설정 > 회원 탈퇴] 실제 동작 연결
- [ ] 앱 서명 키(keystore) 생성 및 안전한 보관

**외부 설정**
- [ ] 🔴 **카카오 개발자 콘솔** — 패키지명 `kr.co.busanquest` 로 변경 + 새 키 해시 등록
      (안 하면 카카오 로그인·지도가 전부 실패한다)
- [ ] 🔴 GitHub Pages 활성화 (Settings → Pages → main / docs)
- [ ] 두 URL 이 브라우저에서 열리는지 확인
- [ ] 리뷰용 테스트 계정 생성 + 미션 이력 채우기

**백엔드**
- [ ] 🔴 `DELETE /api/v1/users/me` (4차 요청서 §2) — 인증 사진 실제 삭제 포함
- [ ] 알림 설정 서버 저장을 하게 되면 §2 표 갱신

**문서**
- [x] 개인정보처리방침 · 이용약관 · 위치기반서비스 약관 작성
- [x] 계정 삭제 페이지 작성
- [ ] 법률 검토 (현재 문서 상단에 "학습·시연 목적" 안내가 들어가 있음)

---

## 7. 문서 수정 방법

약관 문구를 고칠 때는 **`.md` 원본만** 고치고 스크립트를 다시 돌린다.
앱 안에서 보는 문서와 웹 페이지가 어긋나지 않게 하려는 구조다.

```
android/app/src/main/assets/privacy.md    ← 앱 + 웹 공용 원본
android/app/src/main/assets/terms.md
android/app/src/main/assets/location.md
tools/pages/delete-account.md             ← 웹 전용

python tools/build_docs.py                → docs/*.html 재생성
```
