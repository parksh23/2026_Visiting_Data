# 실제 값으로 채운 뒤 파일명을 .env 로 바꿔서 main.py와 같은 폴더에 두세요.
# (.env는 git에 커밋하지 마세요! .gitignore에 추가 권장)

ORACLE_USER=ADMIN
ORACLE_PASSWORD=Sihyeon040215

# Wallet 없이 TCPS(단방향 SSL)로 접속하는 전체 Connection Descriptor.
# 줄바꿈 없이 한 줄로 이어서 넣으세요.
ORACLE_DSN=(description= (retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=1522)(host=adb.ap-osaka-1.oraclecloud.com))(connect_data=(service_name=gf42e2bf77db727_visiting2026_medium.adb.oraclecloud.com))(security=(ssl_server_dn_match=yes)))